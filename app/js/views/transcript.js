define([
  'jquery',
  'underscore', 
  'backbone',
  'models/statement',
  'collections/focused'
	], function($,_,Backbone,
		StatementModel, FocusedCollection){

	var TranscriptView = Backbone.View.extend({
		
		
		
	});

	return TranscriptView;
});